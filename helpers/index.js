const { createSlug } = require('./createSlug')
const { copy } = require('./copy')
const { login } = require('./login')
const { copyUpdate } = require('./copyUpdate')
const { createBlended } = require('./createBlended')
const { unsetUniqueProps } = require('./unsetUniqueProps')

module.exports = {
  createSlug,
  copy,
  login,
  copyUpdate,
  createBlended,
  unsetUniqueProps
}
